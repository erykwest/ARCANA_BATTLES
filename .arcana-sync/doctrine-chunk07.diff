+      return chosen;
+    },
+
+    gridSlots(side) {
+      const z = Deployment.zone(side);
+      const xs = [0.08,0.18,0.28,0.38,0.50,0.62,0.72,0.82,0.92];
+      const depths = [0.88,0.58,0.28]; // 1 = verso il centro del campo
+      const margin = 2.8 * CFG.U;
+      return depths.flatMap((frontness, row) => xs.map((xf, col) => {
+        const x = z.x + margin + (z.w - margin * 2) * xf;
+        const rearY = side === 'blue' ? z.y + z.h - margin : z.y + margin;
+        const frontY = side === 'blue' ? z.y + margin : z.y + z.h - margin;
+        const y = rearY + (frontY - rearY) * frontness;
+        return { x, y, xf, frontness, row, col, key:`${row}:${col}` };
+      }));
+    },
+
+    slotScore(doctrineId, spec, slot, strongFlank) {
+      const r = spec.roles || {};
+      const x = slot.xf;
+      const f = slot.frontness;
+      const center = this.clamp01(1 - Math.abs(x - 0.5) * 2);
+      const edge = 1 - center;
+      const rear = 1 - f;
+      const strong = strongFlank === 'left' ? (1 - x) : x;
+      let score =
+        (r.line || 0) * (1.45*f + 0.55*center) +
+        (r.ranged || 0) * (1.65*rear + 0.65*center) +
+        (r.mobile || 0) * (1.25*edge + 0.45*f) +
+        (r.shock || 0) * (0.85*f + 0.80*edge) +
+        (r.screen || 0) * (0.70*f + 0.55*edge) +
+        (r.reserve || 0) * (1.25*rear + 0.45*center);
+
+      if (doctrineId === 'center_wings') score += (r.line||0)*center*1.25 + (r.mobile||0)*edge*1.70 + (r.shock||0)*edge;
+      if (doctrineId === 'line_reserve') score += (r.line||0)*f*1.35 + (r.reserve||0)*rear*1.70 + (r.ranged||0)*rear*0.70;
+      if (doctrineId === 'wing_concentration') score += strong * (1.0 + (r.mobile||0)*1.2 + (r.shock||0)*1.35 + (r.line||0)*0.45);
+      if (doctrineId === 'double_envelopment') score += (r.mobile||0)*edge*2.15 + (r.shock||0)*edge*1.35 + (r.line||0)*center*0.75;
+      if (doctrineId === 'cavalry_flanks') score += (r.mobile||0)*edge*2.55 + (r.line||0)*center*0.75 + (r.ranged||0)*rear*0.60;
+      if (doctrineId === 'defense_depth') score += (r.reserve||0)*rear*2.0 + (r.ranged||0)*rear + (r.line||0)*(0.65 + 0.35*center);
+      if (doctrineId === 'oblique') {
