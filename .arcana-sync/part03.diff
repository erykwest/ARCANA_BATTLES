       return false;
     },
 
@@ -3270,14 +3423,22 @@
       }
 
       if (t.type === 'pond' || t.type === 'forest') {
-        if (this.containsPoint(t, point, 0)) return 0;
-        const pts = (t.blob || []).map(p => this.localToWorld(t, p));
-        let best = Infinity;
+        const pts = this.blobPoints(t).map(p => this.localToWorld(t, p));
+        let boundaryDist = Infinity;
         for (let i = 0; i < pts.length; i++) {
           const a = pts[i], b = pts[(i + 1) % pts.length];
-          best = Math.min(best, Geometry.pointSegmentDistance(point, a, b));
-        }
-        return best;
+          boundaryDist = Math.min(boundaryDist, Geometry.pointSegmentDistance(point, a, b));
+        }
+
+        const inside = this.polygonContains(t, point.x, point.y, 0);
+        const inset = CFG.TERRAIN_SOFT_EDGE_U * CFG.U;
+
+        // Hard core = spline visibile arretrata verso l'interno.
+        // Fuori: la distanza dal core è bordo + inset.
+        // Nella fascia morbida: inset - profondità dentro il bordo.
+        // Nel core: 0.
+        if (!inside) return boundaryDist + inset;
+        return boundaryDist >= inset ? 0 : inset - boundaryDist;
       }
 
       return Infinity;
@@ -3304,16 +3465,170 @@
 
     placementBlocked(u) { return State.terrain.some(t=>this.blocksUnit(u,t)); },
 
+    poseFitsBounds(u) {
+      const b = Geometry.boundsFor(u);
+      return Geometry.corners(u).every(p =>
+        p.x >= b.minX && p.x <= b.maxX &&
+        p.y >= b.minY && p.y <= b.maxY
+      );
+    },
+
+    poseClearOfUnits(u) {
+      return !State.units.some(o =>
+        o.id !== u.id &&
+        o.status.alive &&
+        !Engagements.pair(u, o) &&
+        Geometry.overlapOBB(u, o)
+      );
+    },
+
+    candidatePoseFree(u, x, y) {
+      const saved = { x:u.pose.x, y:u.pose.y };
+      u.pose.x = x;
+      u.pose.y = y;
+      const free =
+        !this.placementBlocked(u) &&
+        this.poseFitsBounds(u) &&
+        this.poseClearOfUnits(u);
+      u.pose.x = saved.x;
+      u.pose.y = saved.y;
+      return free;
+    },
+
+    recoverUnitFromBlockingTerrain(u, oldPose = null) {
+      if (!u?.status?.alive) return false;
+
+      const current = { x:u.pose.x, y:u.pose.y };
+
+      // Caso normale: il nuovo frame ha penetrato il terreno ma la posa
+      // precedente era ancora valida. Torna semplicemente indietro.
+      if (oldPose && this.candidatePoseFree(u, oldPose.x, oldPose.y)) {
+        u.pose.x = oldPose.x;
+        u.pose.y = oldPose.y;
+        return true;
+      }
+
+      // Caso "stuck": anche oldPose è già penetrata. Cerca la minima
+      // traslazione libera attorno alla posa corrente, senza cambiare angolo.
+      // Step piccoli per correggere anche intrusioni di pochi pixel.
+      const step = 0.20 * CFG.U;
+      const maxRadius = 4.0 * CFG.U;
+      const directions = 32;
+
+      for (let r = step; r <= maxRadius + 1e-6; r += step) {
+        for (let i = 0; i < directions; i++) {
+          const a = (Math.PI * 2 * i) / directions;
+          const x = current.x + Math.cos(a) * r;
+          const y = current.y + Math.sin(a) * r;
+          if (!this.candidatePoseFree(u, x, y)) continue;
+
+          u.pose.x = x;
+          u.pose.y = y;
+          Log.add(`${u.name}: TERRAIN UNSTUCK ${(r / CFG.U).toFixed(1)}U`);
+          return true;
+        }
+      }
+
+      // Ultimo fallback: se non trova nulla entro 4U, torna alla posa
+      // precedente anche se non perfetta. Non teletrasportiamo più lontano.
+      if (oldPose) {
+        u.pose.x = oldPose.x;
+        u.pose.y = oldPose.y;
+      }
+      return false;
+    },
+
+    routHeadingClear(u, angle, probeU = 3.5) {
+      const saved = { x:u.pose.x, y:u.pose.y, angle:u.pose.angle };
+      const stepU = 0.20;
+      let clear = true;
+
+      // Verifica anche la sola rotazione nella nuova direzione:
+      // un reparto lungo può urtare il core pur senza traslare.
+      u.pose.angle = angle;
+      if (this.placementBlocked(u) || !this.poseFitsBounds(u) || !this.poseClearOfUnits(u)) {
+        clear = false;
+      }
+
+      if (clear) {
+        for (let dU = stepU; dU <= probeU + 1e-6; dU += stepU) {
+          u.pose.x = saved.x + Math.cos(angle) * dU * CFG.U;
+          u.pose.y = saved.y + Math.sin(angle) * dU * CFG.U;
+          if (this.placementBlocked(u) || !this.poseFitsBounds(u) || !this.poseClearOfUnits(u)) {
+            clear = false;
+            break;
+          }
+        }
+      }
+
+      u.pose.x = saved.x;
+      u.pose.y = saved.y;
+      u.pose.angle = saved.angle;
+      return clear;
+    },
+
+    findRoutDeflection(u, preferredAngle) {
+      // Cerca prima piccole deviazioni, poi vere tangenti e infine
+      // una fuga quasi opposta. Ogni candidato deve essere libero
+      // lungo TUTTO il tratto, non soltanto al punto finale.
+      const offsetsDeg = [
+        0, 15, -15, 30, -30, 45, -45,
+        60, -60, 75, -75, 90, -90,
+        105, -105, 120, -120, 135, -135, 150, -150
+      ];
+
+      const saved = { x:u.pose.x, y:u.pose.y, angle:u.pose.angle };
+
+      // Se è già tangente al core potrebbe non avere spazio nemmeno per ruotare.
+      // In quel caso crea una piccola camera di manovra arretrando rispetto
+      // alla direttrice di fuga e riprova.
