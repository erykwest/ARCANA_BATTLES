+      const backStepsU = [0, 0.25, 0.5, 0.75, 1.0, 1.25, 1.5, 1.75, 2.0];
+
+      for (const backU of backStepsU) {
+        u.pose.x = saved.x - Math.cos(preferredAngle) * backU * CFG.U;
+        u.pose.y = saved.y - Math.sin(preferredAngle) * backU * CFG.U;
+        u.pose.angle = saved.angle;
+
+        if (this.placementBlocked(u) || !this.poseFitsBounds(u) || !this.poseClearOfUnits(u)) {
+          continue;
+        }
+
+        for (const deg of offsetsDeg) {
+          const a = Geometry.normalizeAngle(preferredAngle + deg * Math.PI / 180);
+          if (this.routHeadingClear(u, a, 3.5)) {
+            if (backU > 0) {
+              Log.add(`${u.name}: ROUT crea spazio di manovra ${backU.toFixed(2)}U`);
+            }
+            return a;
+          }
+        }
+      }
+
+      // Nessuna soluzione: ripristina esattamente la posa iniziale.
+      u.pose.x = saved.x;
+      u.pose.y = saved.y;
+      u.pose.angle = saved.angle;
+      return preferredAngle;
+    },
+
     movementMultiplier(u) {
       if (!u?.status?.alive) return 1;
-      const c=Geometry.center(u); let mult=1, road=false;
+      const c = Geometry.center(u);
+      let mult = 1, road = false;
+
       for (const t of State.terrain) {
-        if (!this.containsPoint(t,c,0)) continue;
-        if (t.type==='rough') mult=Math.min(mult,0.65);
-        else if (t.type==='forest') mult=Math.min(mult,0.5);
-        else if (t.type==='road') road=true;
-      }
-      if (road) mult+=0.5;
+        if (t.type === 'pond' || t.type === 'forest') {
+          // La fascia esterna visibile non blocca: è terreno molto accidentato.
+          if (this.unitTouchesVisualTerrain(u, t)) mult = Math.min(mult, 0.5);
+          continue;
+        }
+
+        if (!this.containsPoint(t, c, 0)) continue;
+        if (t.type === 'rough') mult = Math.min(mult, 0.65);
+        else if (t.type === 'road') road = true;
+      }
+
+      if (road) mult += 0.5;
       return mult;
     },
 
@@ -3354,8 +3669,12 @@
     },
 
     drawShapeBlob(t) {
-      const pts=t.blob||[];
-      pts.forEach((p,i)=>{const w=this.localToWorld(t,p);if(i===0)ctx.moveTo(w.x,w.y);else ctx.lineTo(w.x,w.y);});
+      const pts = this.blobPoints(t);
+      pts.forEach((p,i) => {
+        const w = this.localToWorld(t,p);
+        if (i === 0) ctx.moveTo(w.x,w.y);
+        else ctx.lineTo(w.x,w.y);
+      });
       ctx.closePath();
     },
 
@@ -3415,7 +3734,14 @@
       // Per ostacoli fissi il planner deve garantire che TUTTO il footprint
       // possa ruotare e passare. Usiamo il raggio del rettangolo dal front-pivot
       // solo contro terreno, senza reintrodurre il vecchio collider enorme fra unità.
-      return Math.hypot(d.depth, d.front / 2) + 0.12 * CFG.U;
+      const base = Math.hypot(d.depth, d.front / 2) + 0.12 * CFG.U;
+
+      // Dopo una collisione reale col terreno allarga temporaneamente il corridoio:
+      // evita che A* scelga di nuovo lo stesso waypoint tangente che ha causato lo stuck.
+      const boost = State.simTime < (u.motion.terrainClearanceBoostUntil || 0)
+        ? 0.85 * CFG.U
+        : 0;
+      return base + boost;
     },
 
     excludedObstacle(u, other) {
@@ -3671,10 +3997,14 @@
     advance(u) {
       const d = u.motion.detour;
       if (!d) return false;
-      d.index += 1;
-      if (d.index < d.waypoints.length) return true;
+
+      // Un waypoint A* non è più parte di una polyline "congelata".
+      // Appena raggiunto viene scartato tutto il vecchio percorso:
+      // il prossimo tick ricalcola A* dalla posa reale corrente verso
+      // il target finale, tenendo conto di unità, terreno e orientamento aggiornati.
+      const hadMore = d.index + 1 < d.waypoints.length;
       u.motion.detour = null;
-      return false;
+      return hadMore;
     }
   };
 
@@ -3697,11 +4027,28 @@
     },
 
     prepareTurnForCurrentTarget(u) {
-      // v0.71: il FLIP automatico è rimosso dalla navigazione.
-      // A*, waypoint, lock e pursuit non possono più spostare il front-pivot
-      // tramite un about-face. Il cambio direzione è gestito in update() con
-      // una rotazione sul posto quando l'errore angolare è grande.
-      return;
+      // FLIP reintrodotto SOLO come manovra di navigazione molto conservativa.
+      // Esclusi attack approach, pursuit, retreat, rout e ranged approach:
+      // sono proprio i casi in cui il vecchio flip interferiva con target,
+      // slot e inseguimenti.
+      if (!u?.status?.alive ||
+          u.status.routing ||
+          Engagements.hasAny(u) ||
+          u.order.kind === 'retreat' ||
+          u.order.pursuit?.enemyId ||
+          u.order.approach ||
+          u.ranged?.approachTargetId ||
+          u.motion.turn) return false;
+
+      const target = Pathfinder.currentWaypoint(u) || u.order.target;
+      if (!target) return false;
+
+      const desired = Math.atan2(target.y - u.pose.y, target.x - u.pose.x);
+      const delta = Geometry.shortestDelta(u.pose.angle, desired);
+
+      // Solo vera inversione: sotto 135° continua a ruotare/curvare normalmente.
+      if (Math.abs(delta) < CFG.NAV_FLIP_THRESHOLD) return false;
+      return this.beginFlip(u);
     },
 
     canFlipSafely(u) {
@@ -3719,9 +4066,15 @@
         }
       };
 
-      const { w, h } = worldPx();
